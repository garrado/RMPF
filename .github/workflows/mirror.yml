name: Mirror → visaanapolis/RMPF

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Mirror push to visaanapolis/RMPF
        uses: pixta-dev/repository-mirroring-action@v1
        with:
          target_repo_url: https://github.com/visaanapolis/RMPF.git
          ssh_private_key: ${{ secrets.MIRROR_SSH_KEY }}
